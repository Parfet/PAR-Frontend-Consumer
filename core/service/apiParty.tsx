import api from '../../utils/api'

export const apiParty = {
  createParty: async (data) => {
    console.log(data)
    // const response = await api.post('/party', data)
    // return response
  },
}