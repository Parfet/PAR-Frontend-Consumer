version: '3.8'

services:
 par-fe:
  container_name: PAR-Frontend-Consumer-staging
  ports:
   - 3001:3001
  build:
   context: .
   dockerfile: Dockerfile
  volumes:
   - /app:/usr/src/app/       
   - /usr/src/app/node_modules
   - /usr/src/app/.next
  env_file:
   - .env
   - .env.production